---
id: plan/task/T-002
kind: plan/task
status: completed
created_at: 2026-06-27T18:30:00Z
author: AI Agent
story_ref: S-002
---

# Task: 实现 dispatch 命令

## Task 概述

实现 `spec-graph dispatch` 命令,生成下一步动作清单。

## 关联 Story

- S-002: 调度下一步动作

## 验收标准

- [ ] 读取 machine-state.yaml 和 graph.yaml
- [ ] 调用 computeNextPlan 计算下一步
- [ ] 返回 manifest 包含所有必需字段
- [ ] 支持 `--json` 参数输出 JSON 格式
- [ ] 为 produce_artifact 添加 template_ref/suggested_doc_path/document_guidance
- [ ] 为 run_check 添加 check_command
- [ ] 为 verify_trace 添加 trace_query
- [ ] 暴露 missing_artifacts/failed_checks/missing_traces/missing_contracts

## 实现步骤

1. 创建 `src/commands/dispatch.ts`
2. 读取 graph.yaml 和 machine-state.yaml
3. 调用 `computeNextPlan(graph, state, traceIndex, projectRoot)`
4. 构建 manifest 对象
5. 为每个 action 添加文档指导字段
6. 输出 JSON 或人类可读格式
7. 添加测试用例

## 文件清单

- `src/commands/dispatch.ts` (已存在,增强)
- `src/commands/dispatch.test.ts` (已存在,增强)
- `src/commands/dispatch-doc.test.ts` (新建)

## 技术细节

### Manifest 结构
```typescript
interface DispatchManifest {
  version: string;
  current_stage: string;
  next_stage: string | null;
  blocking_gate: string | null;
  gate_passed: boolean;
  missing_artifacts: string[];
  failed_checks: string[];
  missing_traces: string[];
  missing_contracts: string[];
  forbidden_violations: string[];
  done: boolean;
  actions: DispatchAction[];
}

interface DispatchAction {
  index: number;
  type: SuggestedAction['type'];
  id: string;
  requires_sub_agent: boolean;
  template_ref?: string;
  suggested_doc_path?: string;
  document_guidance?: string;
  check_command?: string;
  trace_query?: TraceQuery;
  // ... other fields
}
```

### 文档指导函数
```typescript
function inferTemplateForArtifact(artifactId: string, graph: Graph): string
function inferDocPathForArtifact(artifactId: string, graph: Graph): string
function inferDocumentGuidance(artifactId: string, graph: Graph): string
```

## 测试方法

```bash
# 运行测试
npx vitest run src/commands/dispatch.test.ts
npx vitest run src/commands/dispatch-doc.test.ts

# 手动测试
spec-graph dispatch --json
```

## 依赖关系

- T-001: 实现 init 命令(已完成)

## 工作量估算

- **估算**: 8 小时
- **实际**: 6 小时

## 完成记录

**完成日期**: 2026-06-27  
**实际工时**: 6 小时  
**状态**: ✅ 已完成
