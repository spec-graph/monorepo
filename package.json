{
  "name": "spec-graph",
  "version": "3.0.0",
  "private": true,
  "description": "spec-graph v3: declaration engine — dispatch manifest generator + gate evaluator. Meta-repo with packages as git submodules.",
  "scripts": {
    "init": "git submodule update --init --recursive",
    "update": "git submodule update --remote --recursive",
    "status": "git submodule status"
  },
  "keywords": [
    "specification",
    "workflow",
    "orchestration",
    "ai-agent",
    "automation",
    "quality-gates",
    "claude-code"
  ],
  "author": "spec-graph team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/spec-graph/spec-graph.git"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
