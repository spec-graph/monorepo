# Cleanup Core Skills — Specifications

This change is a cleanup and architecture alignment. It introduces no new capabilities and modifies no existing spec-level behavior.

- **New Capabilities**: None
- **Modified Capabilities**: None

All changes are:
1. File deletions (`packages/core/skills/`)
2. File moves (SKILL.md → docs/cli-commands/, docs/schemas/)
3. New CLI thin wrappers over existing core APIs (no new API behavior)
4. Entry skill cleanup (documentation and simplification, no spec changes)
