# Aggregates

> 聚合设计:聚合根 + 实体 + 值对象 + 不变量。
> 聚合是数据一致性的边界——一个事务只能修改一个聚合。

## 上下文:{{context_name}}

### 聚合:{{aggregate_name}}

**聚合根**: `{{AggregateRoot}}`

**描述**: {{聚合的业务含义}}

**不变量(Invariants)**:

1. {{INV-001}}: {{必须始终为真的业务规则}}
2. {{INV-002}}: {{必须始终为真的业务规则}}

**实体(Entities)**:

| 实体           | 标识         | 描述            | 生命周期   |
| -------------- | ------------ | --------------- | ---------- |
| {{EntityName}} | {{id_field}} | {{description}} | 跟随聚合根 |

**值对象(Value Objects)**:

| 值对象     | 属性       | 描述            | 验证规则       |
| ---------- | ---------- | --------------- | -------------- |
| {{VOName}} | {{fields}} | {{description}} | {{validation}} |

**允许的操作**:

| 操作       | 输入       | 输出事件        | 触发不变量检查 |
| ---------- | ---------- | --------------- | -------------- |
| {{method}} | {{params}} | {{DomainEvent}} | {{INV-xxx}}    |

---

### 聚合:{{aggregate_name_2}}

**聚合根**: `{{AggregateRoot2}}`

**描述**: {{聚合的业务含义}}

**不变量**:

1. {{INV-001}}: {{必须始终为真的业务规则}}

**实体**:

| 实体           | 标识         | 描述            |
| -------------- | ------------ | --------------- |
| {{EntityName}} | {{id_field}} | {{description}} |

**值对象**:

| 值对象     | 属性       | 描述            |
| ---------- | ---------- | --------------- |
| {{VOName}} | {{fields}} | {{description}} |

---

## 聚合间关系

<!-- 聚合间只能持有 ID 引用,不能直接持有对象引用 -->

| 聚合 A         | 关系 | 聚合 B         | 引用方式                |
| -------------- | ---- | -------------- | ----------------------- |
| {{AggregateA}} | 引用 | {{AggregateB}} | 持有 {{aggregate_b_id}} |

## 一致性边界

| 聚合          | 事务边界 | 跨聚合一致性方式      |
| ------------- | -------- | --------------------- |
| {{aggregate}} | 单事务   | 领域事件 / 最终一致性 |
