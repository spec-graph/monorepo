# spec-graph submit

Submit agent result for gate evaluation

## Usage

```bash
spec-graph submit [options]
```

## Options

  --result <json>
  --result-file <path>
  --session <id>

## Implementation

Backed by: `packages/cli/src/commands/submit.ts`
