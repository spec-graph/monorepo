# spec-graph machine

Direct state machine control

## Usage

```bash
spec-graph machine [options]
```

## Options

  --session
  --init-stage
  --transition
  --json

## Implementation

Backed by: `packages/cli/src/commands/machine.ts`
