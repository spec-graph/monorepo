# spec-graph init

Initialize a new spec-graph project

## Usage

```bash
spec-graph init [options]
```

## Options

  --force
  --stack
  --json

## Implementation

Backed by: `packages/cli/src/commands/init.ts`
