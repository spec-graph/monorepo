# spec-graph sessions

Manage active sessions (list, switch, delete)

## Usage

```bash
spec-graph sessions [options]
```

## Options

  --action
  --session
  --json

## Implementation

Backed by: `packages/cli/src/commands/sessions.ts`
