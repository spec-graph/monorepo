# spec-graph install

Install spec-graph skills into IDE project

## Usage

```bash
spec-graph install [options]
```

## Options

  --ide
  --force
  --local

## Implementation

Backed by: `packages/cli/src/commands/install.ts`
